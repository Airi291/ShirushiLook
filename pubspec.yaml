name: app2
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  camera_macos: ^0.0.9
  flutter_tts: ^4.0.2
  tflite_flutter: ^0.11.0
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/labels_mtsd21.txt
    - assets/labels_mtsd21_ja.txt
    - assets/models/best_float32.tflite
    - assets/models/labels_mtsd_yolo21.txt
    - assets/study/
